(function() {    function Camera() {        this.position = new Vector2D( 0, 0);        this.setPosition = function( pos ) {                this.position.x = pos.x;                this.position.y = pos.y;            };        this.transform = function( pos, outPos ) {                outPos.x = pos.x - this.position.x + CANVAS_WIDTH*0.5;                outPos.y = pos.y - this.position.y + CANVAS_HEIGHT*0.5;            };    }    var TheCamera = new Camera();        window.camera = TheCamera;}()); // make an anonymous global function expression and immediately call it.